================================================================================
                    macOS REINSTALL INSTRUCTIONS
                    Backup Date: January 9, 2026
================================================================================

BACKUP LOCATION: /Volumes/2026/Backups/2026-01-09-settings/
PROJECT LOCATION: /Volumes/2026/Projects/b0ase.com/

================================================================================
STEP 1: INSTALL HOMEBREW
================================================================================

/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Add to PATH (Apple Silicon)
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"

================================================================================
STEP 2: INSTALL HOMEBREW PACKAGES
================================================================================

# Key packages to install first:
brew install git gh node pnpm

# Install all packages from backup:
cat /Volumes/2026/Backups/2026-01-09-settings/dotfiles/homebrew-packages.txt | xargs brew install

# Or install essentials only:
brew install git gh node pnpm postgresql@14 supabase colima docker

================================================================================
STEP 3: RESTORE SSH KEYS (CRITICAL)
================================================================================

mkdir -p ~/.ssh
cp -r /Volumes/2026/Backups/2026-01-09-settings/dotfiles/.ssh/* ~/.ssh/
chmod 700 ~/.ssh
chmod 600 ~/.ssh/hetzner_key
chmod 644 ~/.ssh/hetzner_key.pub
chmod 644 ~/.ssh/config
chmod 644 ~/.ssh/known_hosts

# Test SSH
ssh -T git@github.com

================================================================================
STEP 4: RESTORE GIT CONFIG
================================================================================

cp /Volumes/2026/Backups/2026-01-09-settings/dotfiles/.gitconfig ~/.gitconfig

# Or set manually:
git config --global user.email "richardwboase@gmail.com"
git config --global user.name "b0ase"

# Authenticate GitHub CLI
gh auth login

================================================================================
STEP 5: RESTORE SHELL CONFIGS
================================================================================

cp /Volumes/2026/Backups/2026-01-09-settings/dotfiles/.zshrc ~/.zshrc
cp /Volumes/2026/Backups/2026-01-09-settings/dotfiles/.zprofile ~/.zprofile

# Reload shell
source ~/.zshrc

================================================================================
STEP 6: INSTALL GLOBAL NPM PACKAGES
================================================================================

npm install -g vercel pm2 @anthropic-ai/claude-code

# Or from backup list:
# Check: cat /Volumes/2026/Backups/2026-01-09-settings/dotfiles/npm-globals.txt

================================================================================
STEP 7: RESTORE CLAUDE CODE SETTINGS
================================================================================

mkdir -p ~/.claude
cp -r /Volumes/2026/Backups/2026-01-09-settings/dotfiles/.claude/* ~/.claude/

================================================================================
STEP 8: COPY PROJECT
================================================================================

mkdir -p ~/Projects
cp -r /Volumes/2026/Projects/b0ase.com ~/Projects/

# Or clone fresh and copy .env:
cd ~/Projects
git clone https://github.com/b0ase/b0ase.com.git
cp /Volumes/2026/Projects/b0ase.com/.env.local ~/Projects/b0ase.com/

# Install dependencies
cd ~/Projects/b0ase.com
pnpm install

================================================================================
STEP 9: RESTORE APPLICATION SETTINGS
================================================================================

# Chrome profiles (close Chrome first!)
cp -r /Volumes/2026/Backups/2026-01-09-settings/Chrome ~/Library/Application\ Support/Google/

# Cursor settings (close Cursor first!)
cp -r /Volumes/2026/Backups/2026-01-09-settings/Cursor ~/Library/Application\ Support/

# Antigravity settings
cp -r /Volumes/2026/Backups/2026-01-09-settings/Antigravity ~/Library/Application\ Support/

================================================================================
STEP 10: VERIFY EVERYTHING WORKS
================================================================================

# Test git
cd ~/Projects/b0ase.com
git status
git pull

# Test build
pnpm build

# Test dev server
pnpm dev

================================================================================
BACKUP CONTENTS SUMMARY
================================================================================

/Volumes/2026/Backups/2026-01-09-settings/
├── Antigravity/        (337MB) - Antigravity app settings
├── Chrome/             (7.6GB) - 12+ Chrome profiles
├── Cursor/             (324MB) - Cursor IDE settings
├── Projects/           (5.5GB) - All projects backup
└── dotfiles/
    ├── .ssh/           - SSH keys (hetzner_key)
    ├── .gitconfig      - Git configuration
    ├── .zshrc          - Zsh configuration
    ├── .zprofile       - Zsh profile
    ├── .claude/        - Claude Code settings & CLAUDE.md
    ├── homebrew-packages.txt  - List of 66 Homebrew packages
    └── npm-globals.txt - List of global npm packages

/Volumes/2026/Projects/b0ase.com/
└── Full project with .env.local, node_modules excluded

================================================================================
IMPORTANT NOTES
================================================================================

1. SSH KEYS: The hetzner_key is for server access. Keep permissions at 600.

2. GITHUB AUTH: Run `gh auth login` to re-authenticate GitHub CLI.

3. .ENV FILES: The .env.local contains API keys - don't commit to git!

4. CHROME: If profiles don't restore properly, you may need to sign into
   Google accounts again, but history/bookmarks should be preserved.

5. SUPABASE: May need to run `supabase login` again.

6. VERCEL: May need to run `vercel login` again.

================================================================================
QUICK START (ESSENTIALS ONLY)
================================================================================

# 1. Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"

# 2. Essentials
brew install git gh node pnpm

# 3. SSH
cp -r /Volumes/2026/Backups/2026-01-09-settings/dotfiles/.ssh ~/.ssh
chmod 700 ~/.ssh && chmod 600 ~/.ssh/*_key

# 4. Git
cp /Volumes/2026/Backups/2026-01-09-settings/dotfiles/.gitconfig ~/.gitconfig
gh auth login

# 5. Project
cp -r /Volumes/2026/Projects/b0ase.com ~/Projects/
cd ~/Projects/b0ase.com && pnpm install && pnpm dev

================================================================================
