"use client";

import React from 'react';
import { useStudio } from './studio-context';

// Panel backgrounds for each color theme
const panelBackgrounds: Record<string, string> = {
  black: 'bg-[#050505] border-white/5',
  white: 'bg-gray-100 border-black/10',
  yellow: 'bg-amber-200/80 border-amber-600/20',
  red: 'bg-red-300/80 border-red-700/20',
  green: 'bg-green-300/80 border-green-700/20',
  blue: 'bg-blue-300/80 border-blue-700/20',
};

const Settings = () => {
  const { colorTheme, isDark } = useStudio();

  return (
    <div>
      <div className={`text-xs font-bold uppercase tracking-wider mb-2.5 ${isDark ? 'text-white' : 'text-black'}`}>Settings</div>

      {/* Record/Export */}
      <div className={`p-2 rounded-lg mb-2 border ${panelBackgrounds[colorTheme] || panelBackgrounds.black}`}>
        <div className="flex gap-2">
          <button type="button" className={`flex-1 py-1.5 rounded text-xs transition-colors flex items-center justify-center gap-1 border ${isDark ? 'bg-red-950/30 hover:bg-red-950/50 border-red-900/50 text-red-200' : 'bg-red-100 hover:bg-red-200 border-red-300 text-red-700'}`}>
            <span className="w-2 h-2 bg-red-600 rounded-full animate-pulse"></span> Record
          </button>
          <button type="button" className={`flex-1 py-1.5 rounded text-xs transition-colors border ${isDark ? 'bg-zinc-900 hover:bg-zinc-800 text-zinc-300 border-white/5' : 'bg-gray-200 hover:bg-gray-300 text-gray-700 border-black/10'}`}>🎬 Export</button>
        </div>
      </div>

      {/* Screen Size */}
      <div className={`p-2 rounded-lg mb-2 border ${panelBackgrounds[colorTheme] || panelBackgrounds.black}`}>
        <label className={`block text-xs font-bold mb-2 ${isDark ? 'text-zinc-300' : 'text-gray-700'}`} htmlFor="screen-size">Screen Size</label>
        <select id="screen-size" className={`w-full p-1.5 border rounded text-xs outline-none transition-colors ${isDark ? 'bg-[#0a0a0a] border-white/5 text-white focus:border-white/30' : 'bg-white border-black/10 text-black focus:border-black/30'}`}>
          <option value="1080x1920">1080x1920 (9:16)</option>
          <option value="1920x1080">1920x1080 (16:9)</option>
          <option value="1080x1080">1080x1080 (1:1)</option>
        </select>
      </div>

      {/* Zoom */}
      <div className={`p-2 rounded-lg mb-2 border ${panelBackgrounds[colorTheme] || panelBackgrounds.black}`}>
        <label className={`text-xs font-bold block mb-2 ${isDark ? 'text-zinc-300' : 'text-gray-700'}`}>Zoom</label>
        <div className="mb-2">
          <div className="flex justify-between items-center mb-1">
            <span className={`text-[10px] ${isDark ? 'text-zinc-500' : 'text-gray-500'}`}>Level</span>
            <span className={`text-[10px] ${isDark ? 'text-zinc-400' : 'text-gray-600'}`}>120%</span>
          </div>
          <input id="zoom-level" type="range" min="50" max="200" defaultValue="120" className={`w-full h-1 rounded-lg appearance-none cursor-pointer ${isDark ? 'bg-zinc-800 accent-white' : 'bg-gray-300 accent-black'}`} />
        </div>
        <div className="mb-2">
          <div className="flex justify-between items-center mb-1">
            <span className={`text-[10px] ${isDark ? 'text-zinc-500' : 'text-gray-500'}`}>Speed</span>
            <span className={`text-[10px] ${isDark ? 'text-zinc-400' : 'text-gray-600'}`}>1000ms</span>
          </div>
          <input id="zoom-speed" type="range" min="100" max="5000" defaultValue="4100" step="100" className={`w-full h-1 rounded-lg appearance-none cursor-pointer ${isDark ? 'bg-zinc-800 accent-white' : 'bg-gray-300 accent-black'}`} />
        </div>
      </div>

      {/* Clip Info */}
      <div className={`p-2 rounded-lg mb-2 border ${panelBackgrounds[colorTheme] || panelBackgrounds.black}`}>
        <div className="flex justify-between items-center text-[11px] mb-1">
          <span className={isDark ? 'text-zinc-500' : 'text-gray-500'}>Current Clip:</span>
          <span className={`font-bold ${isDark ? 'text-zinc-300' : 'text-gray-700'}`}>14 / 21</span>
        </div>
        <div className="flex justify-between items-center text-[11px] mb-1">
          <span className={isDark ? 'text-zinc-500' : 'text-gray-500'}>Total Clips:</span>
          <span className={`font-bold ${isDark ? 'text-zinc-300' : 'text-gray-700'}`}>21</span>
        </div>
        <div className="flex justify-between items-center text-[11px]">
          <span className={isDark ? 'text-zinc-500' : 'text-gray-500'}>Resolution:</span>
          <span className={`font-bold ${isDark ? 'text-zinc-300' : 'text-gray-700'}`}>1080x1920</span>
        </div>
      </div>
    </div>
  );
};

export default Settings;
